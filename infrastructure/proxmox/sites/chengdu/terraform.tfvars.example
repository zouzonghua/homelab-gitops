# Copy to terraform.tfvars and paste the complete contents of your .pub file.
# Never paste the private key without the .pub suffix.
ssh_public_key = "ssh-ed25519 AAAA... your-name"

# Keep API credentials outside this file and inject them through the environment.
