apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: linkding
  namespace: linkding
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`linkding.cd.home.arpa`)
      services:
        - name: linkding
          port: 9090
  tls: {}
